name: macOS GUI in Browser (noVNC)

on:
  workflow_dispatch:

jobs:
  macos-web-vnc:
    runs-on: macos-14
    steps:
      - name: Create Admin User & Configure Remote Management
        env:
          VNC_PASSWORD: ${{ secrets.VNC_PASSWORD }}
        run: |
          # 1. Disable Spotlight indexing to free up CPU/disk
          sudo mdutil -i off -a > /dev/null 2>&1 || true

          # 2. Create a clean admin account (avoids existing user dscl auth locks)
          sudo dscl . -create /Users/runneradmin
          sudo dscl . -create /Users/runneradmin UserShell /bin/bash
          sudo dscl . -create /Users/runneradmin RealName "Runner Admin"
          sudo dscl . -create /Users/runneradmin UniqueID 1001
          sudo dscl . -create /Users/runneradmin PrimaryGroupID 80
          sudo dscl . -create /Users/runneradmin NFSHomeDirectory /Users/runneradmin
          sudo dscl . -passwd /Users/runneradmin "$VNC_PASSWORD"
          sudo createhomedir -c -u runneradmin > /dev/null 2>&1 || true
          sudo dscl . -append /Groups/admin GroupMembership runneradmin

          # 3. Configure Apple Remote Management
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure -allowAccessFor -allUsers -privs -all
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -configure -clientopts -setvnclegacy -vnclegacy yes

          # 4. Generate the legacy VNC password hash using macOS XOR key
          echo "$VNC_PASSWORD" | perl -we '
            BEGIN { @k = unpack "C*", pack "H*", "1734516E8BA8C5E2FF1C39567390ADCA"};
            $_ = <>; chomp; s/^(.{8}).*/$1/;
            @p = unpack "C*", $_;
            foreach (@k) { printf "%02X", $_ ^ (shift @p || 0) };
            print "\n"
          ' | sudo tee /Library/Preferences/com.apple.VNCSettings.txt > /dev/null
          sudo chmod 600 /Library/Preferences/com.apple.VNCSettings.txt

          # 5. Restart agent into console session
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -restart -agent -console
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart -activate

      - name: Install noVNC and Cloudflare Tunnel
        run: |
          git clone --depth 1 https://github.com/novnc/noVNC.git /tmp/novnc
          git clone --depth 1 https://github.com/novnc/websockify /tmp/novnc/utils/websockify
          brew install cloudflared

      - name: Launch Web Bridge & Cloudflare Tunnel
        run: |
          # Proxy VNC port 5900 to WebSockets port 6080
          /tmp/novnc/utils/novnc_proxy --vnc localhost:5900 --listen 6080 > /tmp/novnc.log 2>&1 &

          # Start Cloudflare Quick Tunnel (no auth/card required)
          cloudflared tunnel --url http://localhost:6080 --logfile /tmp/cloudflared.log &

          sleep 8

          TUNNEL_URL=$(grep -o 'https://[a-zA-Z0-9-]*\.trycloudflare\.com' /tmp/cloudflared.log | head -n 1)

          echo "================================================================="
          echo "Web Desktop Link:"
          echo "$TUNNEL_URL/vnc.html?autoconnect=true"
          echo "-----------------------------------------------------------------"
          echo "Username: runneradmin"
          echo "Password: (Your configured VNC_PASSWORD secret)"
          echo "================================================================="

      - name: Keep Session Active
        run: |
          sleep 14400
