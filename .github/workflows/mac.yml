name: macOS VM

on:
  workflow_dispatch:

jobs:
  macos-vnc:
    runs-on: macos-latest
    timeout-minutes: 370

    env:
      VNC_USERNAME: runneradmin
      VNC_PASSWORD: ${{ secrets.VNC_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install dependencies
        shell: bash
        run: |
          echo "- Installing cloudflared and displayplacer"
          brew install cloudflared displayplacer

          echo "- Installing numpy for websockify"
          pip3 install --break-system-packages numpy 2>/dev/null || pip3 install numpy 2>/dev/null || true

          echo "- Downloading noVNC and Websockify"
          git clone --depth 1 https://github.com/novnc/noVNC.git /tmp/novnc
          git clone --depth 1 https://github.com/novnc/websockify /tmp/novnc/utils/websockify

      - name: Setup user
        shell: bash
        run: bash scripts/setup.sh

      - name: Optimize macOS
        shell: bash
        run: bash scripts/optimize/main.sh

      - name: Patch TCC permissions
        shell: bash
        run: bash scripts/tcc.sh

      - name: Configure and start VNC server
        shell: bash
        run: bash scripts/vnc.sh

      - name: Start VNC tunnel
        shell: bash
        run: bash scripts/tunnel.sh

      - name: Stream tunnel logs
        shell: bash
        run: bash scripts/logs.sh
